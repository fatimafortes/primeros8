# Copy to .env.local and fill in from the Supabase project dashboard.
# Never commit .env.local — it's gitignored already.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only. Bypasses RLS — used for seeding invented data (feature 2)
# and nowhere in request-handling code. Never expose with NEXT_PUBLIC_.
SUPABASE_SERVICE_ROLE_KEY=
